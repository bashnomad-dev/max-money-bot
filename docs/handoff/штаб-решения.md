# Штаб: разбор Ветки-MMB + решения (11.06)

> Это не хаос — это готовая работа, ждавшая решений штаба. Ниже вердикт после **независимой проверки**.

## Проверка штаба (сделано, не со слов)
- Прогнал тесты сам: **105 passed (1.31s)**. Зелёные подтверждены.
- Объём: 23 файла, +1650 строк. Парсинг (prompts.py, tool_to_domain), STT-фолбэк (stt/factory), упрощение (pipeline/handlers/settings), многопользователь (колонка «Автор»), тесты (test_demo_fixes +149).
- Новых зависимостей нет → `pip install` на сервере не нужен. Откат чистый.
- **Вердикт: работа готова к деплою тест-недели.** Качество ок, риск низкий.

## 3 решения — рекомендации штаба (нужен твой «да»)

**1. Деплой ветки `fix/demo-stt-parsing` на VPS владельца → ДА.**
   - Готово, проверено, откат есть. Деплой делает **владелец** (shell-доступ к серверу только у него; у агента/штаба — нет).
   - Команды уже расписаны в `деплой-тест-недели.md`. Нужны от владельца 2 вещи: **точное имя systemd-сервиса и папка проекта** на VPS.
   - После рестарта — один раз `/setup` боту (миграция колонки «Автор»).

**2. Модель кассира → Вариант 1** (раздельные лички + общая таблица).
   - Без кода, чистая изоляция, «Автор» уже виден. Совпадает с выбором владельца в сессии.
   - Вариант 2 (общий чат, ~полдня кода) и урезанные роли — в бэклог.

**3. Подключение Евгения → НЕ блокер, данные были.**
   - MAX user_id Евгения = **`158102131`** — лежит в `docs/handover-checklist.md` (стр. 15, 48, тестовый id). Ветка написала «данных нет», не грепнув собственный репо.
   - Действие: `ALLOWED_USER_IDS=158102131` в `.env` на VPS. Всё. Никаких запросов Евгению.

## Статус кода
Ветка `fix/demo-stt-parsing` запушена на origin (видна, готова к `git pull` на сервере). Деплой ждёт «да» владельца + имя сервиса/папки. Штаб VPS не трогает.
