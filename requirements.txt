# === MAX Bot API ===
maxapi>=0.4

# === LLM (российский стек) ===
gigachat>=0.1.30          # официальный Python SDK GigaChat от Сбера

# === STT ===
gigaam>=2.0               # локальная модель распознавания речи (Сбер), бесплатная
# при невозможности установить gigaam — fallback на OpenAI Whisper API
openai>=1.40              # для fallback Whisper и опц. эмбеддингов

# === Google Sheets ===
gspread>=6.1
google-auth>=2.30

# === Конфиг и валидация ===
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# === HTTP ===
httpx>=0.27

# === Парсинг данных (импорт прайса) ===
xlrd>=2.0                 # для .xls (Прайс 2025)
openpyxl>=3.1             # для .xlsx
rapidfuzz>=3.6            # fuzzy matching для канонизации SKU и контрагентов

# === Аудио ===
# ffmpeg-python — обёртка вокруг ffmpeg (нужен сам ffmpeg в PATH)
ffmpeg-python>=0.2

# === Планировщик ===
apscheduler>=3.10

# === Dev ===
ruff>=0.5
mypy>=1.10
pytest>=8.0
pytest-asyncio>=0.23
