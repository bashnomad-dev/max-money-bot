# === MAX Bot API ===
maxapi>=0.4

# === LLM (российский стек) ===
gigachat>=0.1.30          # официальный Python SDK GigaChat от Сбера

# === STT ===
# GigaAM ставится НЕ из PyPI (там только устаревший 0.1.0), а из git — см. deploy/setup-vps.sh:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   pip install git+https://github.com/salute-developers/GigaAM.git
# gigaam импортит torchaudio, поэтому нужны ОБА (torch+torchaudio), CPU-сборка.
# Без них бот работает в text-only режиме (импорт gigaam ленивый); fallback на Whisper API ниже.
openai>=1.40              # для fallback Whisper и опц. эмбеддингов

# === Google Sheets ===
gspread>=6.1
google-auth>=2.30

# === Конфиг и валидация ===
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# === HTTP ===
httpx>=0.27

# === Парсинг данных (импорт прайса) ===
xlrd>=2.0                 # для .xls (Прайс 2025)
openpyxl>=3.1             # для .xlsx
rapidfuzz>=3.6            # fuzzy matching для канонизации SKU и контрагентов

# === Аудио ===
# ffmpeg-python — обёртка вокруг ffmpeg (нужен сам ffmpeg в PATH)
ffmpeg-python>=0.2

# === Планировщик ===
apscheduler>=3.10

# === Dev ===
ruff>=0.5
mypy>=1.10
pytest>=8.0
pytest-asyncio>=0.23
